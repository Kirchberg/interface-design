#!/bin/bash
set -euo pipefail

# Verify generated install surfaces stay synchronized with the canonical skill.
# Run `scripts/sync-install-surfaces` to refresh generated files before commit.
scripts/sync-install-surfaces --check
